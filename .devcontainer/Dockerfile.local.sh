#!/bin/bash
# Project-specific build steps — runs as root during Docker build.
# Add project image setup here: apt packages, global tools, etc.
# This file is never overwritten by devcontainer-init --update.
#
# Example:
#   apt-get install -y postgresql-client
